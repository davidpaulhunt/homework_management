%h1
  Active Enrollments

%table.table.table-striped.table-hover
  %thead
    %th What
    %th Where
    %th When
    %th
    %th
    %th
  %tbody
    - current_user.active_enrollments.each do |enrollment|
      = render enrollment
%br
%br
%h3
  My Enrollments
  = link_to "New Enrollment", new_student_enrollment_path(current_user.id), class: "btn btn-xs btn-success"

%table.table.table-striped.table-hover
  %thead
    %th What
    %th Where
    %th When
    %th
    %th
    %th
  %tbody
    = render current_user.enrollments unless nil

%br
%br
.row
  .col-md-4
    %h3 Where You Can Find Us
    %legend
    - unless Location.all.count == 0
      %ul
        - Location.all.each do |location|
          %li
            %div.btn.btn-xs.btn-list
              = location.where
          %br
  .col-md-4
    %h3 Courses We Provide
    %legend
    - unless Course.all.count == 0
      %ul
        - Course.all.each do |course|
          %li
            %div.btn.btn-xs.btn-list
              = course.title
          %br
    %br
  .col-md-4
    %h3 Upcoming Cohorts
    %legend
    - unless Cohort.all.count == 0
      %ul
        - Cohort.all.each do |cohort|
          %li
            %div.btn.btn-xs.btn-list
              = cohort.listing
            %div.hidden-date= cohort.start_date
          %br